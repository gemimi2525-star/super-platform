# Super Platform - Environment Variables

# ===========================================
# Firebase Client Configuration (Public)
# ===========================================
# These variables are exposed to the browser
# Get these from Firebase Console > Project Settings > General
NEXT_PUBLIC_FIREBASE_API_KEY=your-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.firebasestorage.app
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789012
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# ===========================================
# Firebase Admin SDK (Server-Only - CRITICAL)
# ===========================================
# Get these from Firebase Console > Project Settings > Service Accounts
# ⚠️ NEVER commit these values to git!
# ⚠️ NEVER expose these on client-side!
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour-Private-Key-Here\n-----END PRIVATE KEY-----"

# ===========================================
# Application Configuration
# ===========================================
# Environment: development | staging | production
NODE_ENV=development

# ===========================================
# Security & Secrets
# ===========================================
# CRON secret for scheduled jobs
# Generate: openssl rand -base64 32
CRON_SECRET=your-secure-random-string-here

# Super Admin User ID (Server-only recommended)
# ⚠️ Consider moving to server-only (remove NEXT_PUBLIC_ prefix)
NEXT_PUBLIC_SUPER_ADMIN_ID=your-admin-user-id

# ===========================================
# Development Only (REMOVE IN PRODUCTION)
# ===========================================
# ⚠️ WARNING: These should NEVER be enabled in production!
# Auth bypass for development (allows testing without Firebase Admin)
# AUTH_DEV_BYPASS=true  # ← MUST be false or removed in production!

# Auth mode (legacy - consider removing)
# NEXT_PUBLIC_AUTH_MODE=localStorage

# ===========================================
# Test Login (DEV/Preview Only)
# ===========================================
# ⚠️ NEVER enable ENABLE_TEST_LOGIN in production!
# ⚠️ This feature shows test credentials for easy browser testing
# Enable test login page at /v2/test-login
ENABLE_TEST_LOGIN=true

# Test account credentials (replace with your test accounts)
TEST_OWNER_EMAIL=owner@example.com
TEST_OWNER_PASSWORD=test-password-owner

TEST_ADMIN_EMAIL=admin@example.com
TEST_ADMIN_PASSWORD=test-password-admin

TEST_USER_EMAIL=user@example.com
TEST_USER_PASSWORD=test-password-user

# ===========================================
# Optional Services
# ===========================================
# OpenAI API (if using AI features)
# OPENAI_API_KEY=sk-...

# ===========================================
# How to Use This File
# ===========================================
# 1. Copy this file: cp .env.example .env.local
# 2. Fill in your actual values
# 3. Never commit .env.local to git (it's in .gitignore)
# 4. For production, set these in your hosting platform's environment variables

# ===========================================
# Important Security Notes
# ===========================================
# - NEXT_PUBLIC_* variables are visible in browser (use only for public data)
# - Server-only variables (without NEXT_PUBLIC_) are never exposed to client
# - Rotate secrets regularly (every 90 days recommended)
# - Use strong, random values for all secrets
# - Keep production credentials separate from development
