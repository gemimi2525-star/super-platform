/**
 * Core OS Home Page
 * 
 * Direct entry point to the OS Shell (with Lock Screen).
 */

import { OSShell } from '@/components/os-shell';

export default function HomePage() {
    return <OSShell />;
}
